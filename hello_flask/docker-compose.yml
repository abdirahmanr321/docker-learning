services:
  web:
    build:
      context: .
    image: my-flask-app:multistage
    ports:
      - "5003:5003"
    depends_on:
      - mydb

  mydb:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: my-secret-pw